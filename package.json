{
  "name": "@vizuh/clicktrail-mcp",
  "version": "0.1.0",
  "description": "Dependency-light MCP stdio server for ClickTrail attribution schemas, diagnostics, validation, and reconciliation.",
  "type": "module",
  "bin": { "clicktrail-mcp": "./dist/index.mjs" },
  "files": ["dist", "server.json", "README.md", "LICENSE"],
  "scripts": { "build": "node scripts/build.mjs", "test": "node --test tests/*.test.mjs", "typecheck": "node --check src/index.mjs && node --check src/tools.mjs" },
  "keywords": ["mcp", "attribution", "offline-conversions", "google-ads", "meta-capi", "clicktrail"],
  "publishConfig": { "access": "public" },
  "license": "MIT",
  "author": "Vizuh OÜ",
  "repository": { "type": "git", "url": "https://github.com/vizuh/clicktrail-mcp" },
  "engines": { "node": ">=20" }
}
